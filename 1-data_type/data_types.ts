// type number

let number:Number = 10;

console.log(number);

// type string

let firstName:string = "John";
let lastName:string = "Doe";

console.log(firstName + " " + lastName);

// type boolean

let isActivated:boolean = true;

// type null

let isNull:null = null;

// type undefined

let isUndefined:undefined = undefined;

// type array

let collection: number[] = [1,2,3,4,5];

console.log(collection);

let collectionOfPrimaries: Array<String> = ["John", "Doe"];

console.log(collectionOfPrimaries);

// type tuple

let newTuples: [string, number, boolean] = ["John", 10, true];

console.log(newTuples);